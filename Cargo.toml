[workspace]
members  = ["space", "algebra"]
resolver = "2"

[workspace.dependencies]
# Local dependencies
harness-algebra = { path = "algebra", version = "=0.2.3" }
harness-space   = { path = "space", version = "=0.3.0" }

# External dependencies
itertools  = { version = "0.14" }
num-traits = { version = "0.2" }

# Development dependencies
tempfile = { version = "3.19" }
