[workspace]
members  = ["space", "algebra"]
resolver = "2"

[workspace.dependencies]
# Local dependencies
harness-algebra = { path = "algebra" }
harness-space   = { path = "space" }

# External dependencies
itertools  = { version = "0.14" }
num-traits = { version = "0.2" }
termgraph  = { version = "0.4" }
